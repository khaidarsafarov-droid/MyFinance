ktor {
    deployment {
        host = "0.0.0.0"
        host = ${?HOST}
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.truckerload.backend.ApplicationKt.module ]
    }
}
