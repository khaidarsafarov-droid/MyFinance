# LOCAL_ONLY_MODE=true keeps cloud workers/Supabase off and shows "Continue offline".
# Google Sign-In + Google Drive backup still work (Drive does not need SYNC_BACKEND_URL).
# Set false for production builds that also use Ktor/Supabase multi-device sync.
LOCAL_ONLY_MODE=false

GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
# Free tier: enable only "Maps SDK for Android" in Google Cloud (not Directions API).
# Friends map route lines use OSRM (free, no key).
# Bot secrets are NOT embedded into debug or release APKs by default.
# For on-device Telegram debugging only:  ./gradlew :app:assembleDebug -PallowDebugSecrets=true
TELEGRAM_BOT_TOKEN=

# Cloud snapshot/inbox API. Production URLs must use HTTPS.
# Blank = no Ktor (local-first). Example droplet:
# SYNC_BACKEND_URL=https://107.170.0.183.sslip.io
SYNC_BACKEND_URL=
# Durable photo/scan upload and cross-device hydration. Disabled unless explicitly true;
# also requires SYNC_BACKEND_URL, LOCAL_ONLY_MODE=false, and an authenticated JWT session.
CLOUD_MEDIA_ENABLED=false
# device = local foreground Telegram bot; server = authenticated backend inbox.
TELEGRAM_SYNC_MODE=device
# Non-secret username used to build https://t.me/<bot>?start=<link-token>.
TELEGRAM_SERVER_BOT_USERNAME=

# Google OAuth — Web client for ID tokens; Android client registered in Cloud Console
# (package com.truckorig + signing SHA-1). Required for "Sign in with Google".
GOOGLE_WEB_CLIENT_ID=842861516910-gkhu4dh9tu5rc8re40rpe4583hvs4uhv.apps.googleusercontent.com
GOOGLE_ANDROID_CLIENT_ID=842861516910-usd22msidljtngc00p2jeokn6qf5te94.apps.googleusercontent.com

# Supabase — optional. Empty = Google identity is device-local; Drive still syncs.
# Set both for shared cloud account across phones/tablets.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Google Drive backup (Settings → Google Drive): enable Drive API in Google Cloud
# for the same project as GOOGLE_WEB_CLIENT_ID / Android OAuth client.
# After Google login, open Settings and link Drive (drive.appdata scope).

# Do not run another long-poll/webhook with the same TELEGRAM_BOT_TOKEN —
# Telegram delivers updates to only one client.
